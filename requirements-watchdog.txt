# Migration Plugin Watchdog - Python dependencies
# This file lists all dependencies for the Watchdog agent pipeline
# and the Dashboard Lambda. Install with: pip install -r requirements-watchdog.txt

# --- Runtime dependencies ---

# Strands Agents SDK for LLM-powered analysis, review, and refactoring agents
strands-agents>=1.38.0
strands-agents-tools>=0.5.2

# AWS SDK for DynamoDB, Bedrock, Pricing API, KMS, and other AWS services
boto3>=1.42.0

# Dashboard web framework (single Lambda serving HTML + API)
fastapi>=0.115.0

# Lambda adapter for ASGI frameworks (FastAPI on Lambda)
mangum>=0.21.0

# HTML templating for server-side rendered dashboard pages
jinja2>=3.1.0

# Async HTTP client for fetching authoritative sources (Gemini, OpenAI, blogs)
httpx>=0.27.0

# GitHub App JWT generation for short-lived installation tokens
PyJWT>=2.9.0

# Cryptographic operations for JWT signing with GitHub App private key
cryptography>=43.0.0

# Date math for dismissal cooldown calculations (dismissed_at + 2 months)
python-dateutil>=2.9.0

# --- Dev/test dependencies ---

# Property-based testing framework for universal correctness properties
hypothesis>=6.100.0

# Test runner
pytest>=9.0.0

# Async test support for testing async pipeline components
pytest-asyncio>=0.24.0

# AWS service mocking for DynamoDB, Bedrock, and other AWS services in tests
moto[dynamodb]>=5.1.0
